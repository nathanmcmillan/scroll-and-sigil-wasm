package main

import "math"

// Constants
const (
	Pi        = float32(math.Pi)
	HalfPi    = float32(math.Pi * 0.5)
	QuarterPi = float32(math.Pi * 0.25)
	Tau       = float32(math.Pi * 2.0)
	RadToDeg  = float32(180.0 / math.Pi)
	DegToRad  = float32(math.Pi / 180.0)
)
